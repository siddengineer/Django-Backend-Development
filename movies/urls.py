# from django.urls import path
# from . import views

# urlpatterns = [
#     path('lazy-demo/', views.lazy_demo),
# ]
from django.urls import path
from . import views

urlpatterns = [
    path('lazy-demo/', views.lazy_demo),
]
